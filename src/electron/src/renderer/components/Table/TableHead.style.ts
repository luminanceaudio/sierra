import styled from 'styled-components';

export const StyledTableHead = styled.th`
  font-size: 14px;
  font-weight: bold;
  padding: 0;

  > button {
    display: flex;
    align-items: center;
    gap: 2px;
    font-weight: inherit;
    font-size: inherit;
  }
`;
